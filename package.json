{
  "name": "schmaker-tampermonkey",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "prebuild": "node scripts/embed-assets.mjs",
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
